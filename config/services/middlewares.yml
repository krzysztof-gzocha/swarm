http:
  middlewares:

    secured:
      chain:
        middlewares:
          - internal-network
          - redirect-to-https
          - auth-users
          - security-headers

    redirect-to-https:
      redirectScheme:
        scheme: https
        permanent: true
        port: 443

    internal-network:
      ipWhiteList:
        sourceRange:
          - "127.0.0.1/24" # localhost
          - "172.0.0.1/8" # all docker containers

    small-rate-limit:
      rateLimit:
        average: 10
        burst: 10

    big-rate-limit:
      rateLimit:
        average: 100
        burst: 50

    auth-users:
      basicAuth:
        removeHeader: true
        realm: basic
        usersFile: "/configuration/users"

    security-headers:
      headers:
        frameDeny: true
        sslRedirect: true
        contentTypeNosniff: true
        browserXssFilter: true
